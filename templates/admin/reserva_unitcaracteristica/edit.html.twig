{% extends 'admin/base_sonata/edit.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

    <style>
        .medios-collection-wrapper td[class*="medios-prioridad"] {
            white-space: nowrap;
            text-align: center;
        }

        /* Ocultamos el número, se actualiza por JS */
        .medios-collection-wrapper td[class*="medios-prioridad"] input {
            display: none;
        }

        .medios-collection-wrapper td[class*="medios-prioridad"] .sonata-ba-sortable-handler {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 22px;
            height: 22px;
            cursor: grab;
            font-size: 18px;
            color: #666;
            -webkit-user-select: none;
            user-select: none;
            touch-action: pan-y; /* permite scroll vertical */
        }

        .medios-collection-wrapper td[class*="medios-prioridad"] .sonata-ba-sortable-handler:active {
            cursor: grabbing;
        }

        .medios-collection-wrapper tr.ui-sortable-helper {
            background: #fffceb !important;
            box-shadow: 0 3px 8px rgba(0,0,0,0.15);
        }


    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script type="text/javascript">

    </script>

{% endblock %}
