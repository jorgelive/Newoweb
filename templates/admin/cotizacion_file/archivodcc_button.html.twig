{% if object is defined and admin.isGranted('show', object) and admin.hasRoute('archivodcc') and object.id is not null %}
    <li>
        <a class="sonata-action-element" href="{{ admin.generateObjectUrl('archivodcc', object) }}">
            <i class="fa fa-info" aria-hidden="true"></i>
            F DCC
        </a>
    </li>
{% endif %}
