framework:
    messenger:
        routing:
            # 🔥 2. Redirigimos solo este mensaje al transporte síncrono en dev.
            # Los demás (como RunExchangeTaskDispatch) seguirán en async.
            App\Pms\Dispatch\ProcessBeds24WebhookDispatch: sync